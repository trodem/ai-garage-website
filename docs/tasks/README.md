# docs/tasks/

Active feature briefs for **031_GarIQ_Website** (Next.js marketing site).

Same lifecycle as the mobile app repo: planning agent writes the brief; coder agent implements step-by-step; user deletes the brief file when done.

Template and quality rules: see `031_ai_garage_app/docs/tasks/README.md` (adapt paths and verification to this repo).

## Verification defaults (this repo)

- `npm run build` (repository root)
- `npx tsc --noEmit` if TypeScript check is not already part of build
